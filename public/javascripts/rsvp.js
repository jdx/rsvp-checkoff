var RSVP = new Ext.Application({
	name: 'rsvp',
	useLoadMask : true,
	launch : function () {
	
	}
})

launch: function () {

rsvp.views.viewport = new Ext.Panel({
fullsceen: true,
html:'This is the viewport'
});
}